{
  "name": "close-matching-issues",
  "version": "0.0.0",
  "private": true,
  "description": "A GitHub Action for closing issues matching a search query",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc",
    "ci": "npm run build && npm run lint && npm test",
    "format": "prettier --write **/*.ts **/*.md **/*.yaml",
    "lint": "prettier --check **/*.ts **/*.md **/*.yaml",
    "pack": "ncc build",
    "release": "npm run build && npm run format && npm run pack && npm test",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lee-dohm/close-matching-issues.git"
  },
  "keywords": [
    "actions",
    "node",
    "setup"
  ],
  "author": "Lee Dohm",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.2",
    "@actions/github": "^2.1.1"
  },
  "devDependencies": {
    "@types/jest": "^25.1.3",
    "@types/node": "^13.7.6",
    "@zeit/ncc": "^0.21.1",
    "jest": "^25.1.0",
    "jest-circus": "^25.1.0",
    "nock": "^12.0.1",
    "prettier": "1.19.1",
    "ts-jest": "^25.2.1",
    "typescript": "^3.8.3"
  }
}
